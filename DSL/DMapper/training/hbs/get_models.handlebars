[
    {{#each records}}
        {
            "id": "{{ this.fileName }}",
            "name": "{{ this.fileName }}",
            "lastTrained": "{{ this.trainedDate }}",
            "state": "{{ this.state }}",
            "versionNumber": "{{ this.versionNumber }}",
            "description": "{{ this.description }}"
        }{{#unless @last}},{{/unless}}
    {{/each}}
]
