{
    "id": "{{intents.intents.0._source.id}}",
    "examples": [
    {{#each intents.intents.0._source.examples}}
        "{{{escapeQuotes this}}}"{{#unless @last}},{{/unless}}
    {{/each}}
    ],
    "inModel": {{isInModel intents.intents.0._source.intent intents}},
    "serviceId": "{{findConnectedServiceId intents.intents.0._source.intent intents}}",
    "isForService": {{#if (getObjectKeyFromObjectArray intents/intentsModifiedAt 'intent' intents.intents.0._source.intent 'isforservice')}} {{getObjectKeyFromObjectArray intents/intentsModifiedAt 'intent' intents.intents.0._source.intent 'isforservice'}} {{else}} false {{/if}},
    "modifiedAt": "{{intents.intentsModifiedAt.0.created}}"
}
