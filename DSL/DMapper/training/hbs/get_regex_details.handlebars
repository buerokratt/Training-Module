{{#if hits.0._source.regex}}
    {{#if examples}}
        {
        "name": "{{hits.0._source.regex}}",
        "examples": [
        {{#each examplesArray }}
            "{{{this}}}"{{#unless @last}},{{/unless}}
        {{/each}}
        ],
        "id": "{{hits.0._source.regex}}"
        }
    {{else}}
        true
    {{/if}}
{{else}}
    false
{{/if}}