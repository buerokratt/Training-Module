{
    "entities": [
    {{#each data.hits}}
        {{#each this.fields.filtered_items.[0].response}}
            {
                "id": "{{{ this }}}",
                "name": "{{{ this }}}"
            }
            {{#unless @last}},{{/unless}}
        {{/each}}
    {{/each}}
    ]
}