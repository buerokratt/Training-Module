{
  "rules": [
    {{#each hits}}
      {{#each _source.steps as |step|}}
        {{#if step.action}}
          "{{_source.rule}}"
          {{#unless @last}},{{/unless}}
        {{else}}
          {{#if step.active_loop}}
            "{{_source.rule}}"
            {{#unless @last}},{{/unless}}
          {{/if}}
        {{/if}}
      {{/each}}
    {{/each}}
  ]
}
