{
  "regexes": [
    {{#each hits}}
      {
       "regex": "{{_source.regex}}",
       "examples": [
         {{#each _source.examples}}
          "{{this}}"{{#unless @last}},{{/unless}}
         {{/each}}
        ]
      }{{#unless @last}},{{/unless}}
    {{/each}}
  ]
}
