{
    "trigger": "{{{ cronExpression }}}",
    "type": "{{ type }}",
    "command": "{{ command }}",
    "allowedEnvs": [{{#each allowedEnvs}}"{{this}}"{{#unless @last}},{{/unless}}{{/each}}],
    "startDate": {{ startDate }}
}
