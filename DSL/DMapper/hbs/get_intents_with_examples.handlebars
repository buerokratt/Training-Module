{
"intents": [
{{#each hits}}
    {
    "id": "{{_source._id}}",
    "title": "{{_source.intent}}",
    "examples": [
    {{#each _source.examples}}
        "{{{ this }}}"{{#unless @last}},{{/unless}}
    {{/each}}
    ]
    }{{#unless @last}},{{/unless}}
{{/each}}
]
}
