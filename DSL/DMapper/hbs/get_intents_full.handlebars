{{!-- todo check usages of this --}}
{
"intents": [
{{#each intents.intents}}
    {
    "id": "{{id}}",
    "title": "{{title}}",
    "examples": [
    {{#each examples}}
        "{{{this}}}"{{#unless @last}},{{/unless}}
    {{/each}}
    ],
    "inmodel": {{isInModel title ../intents}},
    "serviceId": "{{findConnectedServiceId title ../intents}}",
    "isForService": "{{getObjectKeyFromObjectArray ../intents/intentsModifiedAt 'intent' title 'isforservice'}}",
    "modifiedAt": "{{findModifiedAt title ../intents/intentsModifiedAt}}",
    "count": {{getCount title ../intents}}
    }{{#unless @last}},{{/unless}}
{{/each}}
]
}
