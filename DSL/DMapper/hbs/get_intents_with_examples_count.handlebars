{
  "intents": [
      {{#each buckets}}
        {
         "id": "{{key}}",
         "examples_count": {{examples_counts.value}},
         "inModel": {{isInModel key ../intents}}
        }{{#unless @last}},{{/unless}}
      {{/each}}
    ]
}
