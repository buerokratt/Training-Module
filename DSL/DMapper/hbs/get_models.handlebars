[
    {{#each records}}
        {
            "id": "{{ this.fileName }}",
            "name": "{{ this.fileName }}",
            "lastTrained": "{{ this.trainedDate }}",
            "state": "{{ this.state }}",
            "versionNumber": "{{ this.versionNumber }}"
        }{{#unless @last}},{{/unless}}
    {{/each}}
]