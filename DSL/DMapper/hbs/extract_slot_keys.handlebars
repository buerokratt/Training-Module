{
"slots": [
{{#each hits}}
    {{#each (extractSlotKeys _source.slots)}}
        {"id":"{{this}}","name":"{{this}}"}{{#unless @last}},{{/unless}}
    {{/each}}{{#unless @last}},{{/unless}}
{{/each}}
]
}