{
    "entities": [
    {{#each data}}
        {{#each this.fields.paginated_entities}}
            {
                "id": "{{{ this }}}",
                "name": "{{{ this }}}"
            }
            {{#unless @last}},{{/unless}}
        {{/each}}
    {{/each}}
    ]
}