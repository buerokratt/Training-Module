{
    "entities": [
    {{#each data.hits}}
        {{#each this._source.entities}}
            {
                "id": "{{{ this }}}",
                "name": "{{{ this }}}"
            }
            {{#unless @last}},{{/unless}}
        {{/each}}
    {{/each}}
    ]
}