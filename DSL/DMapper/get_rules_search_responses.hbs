{
  "rules": [
    {{#each hits}}
      {{#each _source.steps as |step|}}
        {{#if step.action}}
          "{{_source.rule}}"
          {{#unless @last}},{{/unless}}
        {{/if}}
      {{/each}}
    {{/each}}
  ]
}
