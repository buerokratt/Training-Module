{
  "stories": [
    {{#each hits}}
      {{#each _source.steps as |step|}}
        {{#if step.action}}
          "{{_source.story}}"
          {{#unless @last}},{{/unless}}
        {{/if}}
      {{/each}}
    {{/each}}
  ]
}
