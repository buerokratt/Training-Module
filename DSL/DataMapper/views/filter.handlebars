{{#each (filterEvents events Filters)}}
  {
  "title": "{{title}}",
  "districts": "{{districts}}",
  "events": [
    {{#each events}}
      {
        "start_string": "{{start_string}}",
        "end_string": "{{end_string}}",
        "location_string": "{{location_string}}"
      }
      {{#unless @last}},{{/unless}}
    {{/each}}
  ]
  }
{{/each}}
