{
  "responses": [
    {{#each hits}}
    {{#if (eq this._id "responses") }}
        {{#each this._source.responses}}
          {"name": {{{toJSON @key}}},
           "response": {{{toJSON this}}}
          }{{#unless @last}},{{/unless}}
        {{/each}}
      {{/if}}
    {{/each}}
  ]
}
