[
    {{#each appeals}}
        {
            "id": "{{ id }}",
            "appeal": "{{ name }}",
            "intent": "{{ intent }}",
            "status: "{{ status }}"
        }
    {{#unless @last}},{{/unless}}
    {{/each}}
]