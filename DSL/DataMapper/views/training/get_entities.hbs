{
    "entities": [
    {{#each hits}}
        {{#if (eq this._index "entities") }}
            {
              "id": "{{ this._id }}",
              "name": "{{ this._source.entity }}"
            }
            {{#unless @last}},{{/unless}}
        {{/if}}
    {{/each}}
    ]
}
