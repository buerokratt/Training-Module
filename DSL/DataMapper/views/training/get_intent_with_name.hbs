{
  "intents": [
    {{#each hits}}
      {
       "intent": "{{_source.intent}}",
       "examples": [
         {{#each _source.examples}}
          "{{this}}"{{#unless @last}},{{/unless}}
         {{/each}}
        ]
      }{{#unless @last}},{{/unless}}
    {{/each}}
  ]
}
