{
"intents": [
{{#each intents}}
    {
    "title": "{{title}}",
    "examples": [{{#each examples}}"{{this}}"{{#unless @last}},{{/unless}}{{/each}}],
    "inModel": {{isInModel title ../inmodel}},
    "count": {{getCount title ../count}}
    }{{#unless @last}},{{/unless}}
{{/each}}
]
}
