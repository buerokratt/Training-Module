[
    {{#each records}}
        {
            "id": "{{ this.fileName }}",
            "name": "{{ this.fileName }}",
            "lastTrained": "{{ this.trainedDate }}",
            "active": "{{modelIsActive this.state}}"
        }{{#unless @last}},{{/unless}}
    {{/each}}
]