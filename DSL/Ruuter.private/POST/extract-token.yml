extractTokenData:
  template: mock-tim-custom-jwt-userinfo
  requestType: post
  headers:
    cookie: ${incoming.headers.cookie}
  body:
    cookieName: "customJwtCookie"
  result: jwtResult

returnResult:
  return: ${jwtResult}
