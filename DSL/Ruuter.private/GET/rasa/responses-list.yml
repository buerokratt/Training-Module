declaration:
  call: declare
  version: 0.1
  description: "Decription placeholder for 'RESPONSES-LIST'"
  method: get
  accepts: json
  returns: json
  namespace: training
  allowlist:
    params:
      - field: name
        type: string
        description: "Response name"

getResponses:
  call: http.post
  args:
    url: "[#TRAINING_OPENSEARCH]/responses/_search/template"
    body:
      id: "response-by-intent-name"
      params:
        response_name: "utter_common_teenus_citizien_initiative_popular"
  result: getResponsesResult

mapResponsesData:
  call: http.post
  args:
    url: "[#TRAINING_DMAPPER]/hbs/training/get_responses_list"
    headers:
      type: "json"
    body:
      response: ${getResponsesResult.response.body.hits.hits}
  result: responsesData
  next: returnSuccess

returnSuccess:
  return: ${responsesData.response.body.responses}
  wrapper: false
  next: end
